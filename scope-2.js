//Global (name)
// local variable
  // local variable
// local variable  
let name = "Ghislain"

if (true) {
    let name = "Mitahi"
    if (true) {
        name  = "nicole"
        console.log(name);
    }
}
if (true) {
    console.log(name)
}